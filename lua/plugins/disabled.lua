-- LazyVim-bundled plugins we don't use; kept disabled here instead of removed
-- upstream so `Lazy clean` doesn't fight LazyVim's own spec every update.
return {
	{ "folke/flash.nvim", enabled = false },
	{ "MagicDuck/grug-far.nvim", enabled = false },
	{ "folke/persistence.nvim", enabled = false },
}
